package fx;
import Protocol;
import mt.bumdum9.Lib;


class GyroAutoSpawn extends mt.fx.Sequence {

	public function new() {
		super();
	}

	override function update() {
		super.update();
		if ( timer % 40 == 0 ) {
		}
	}
}
