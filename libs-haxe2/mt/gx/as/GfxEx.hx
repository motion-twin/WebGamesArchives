package mt.gx.as;
import flash.display.Graphics;
import mt.gx.math.Vec2;

class GfxEx {
	/* sh*t this does not work
	public static inline function addCircle<T>(
		o : { graphics:Graphics } ,
		cx=0.,
		cy=0.,
		radius=1.,
		color= 0xFFffFF,
		alpha=1.,
		?line:{
		thick : Float,
		alpha :Float,
		color : Int }
		
	){
		if( line == null)
			o.graphics.lineStyle();
		else 
			o.graphics.lineStyle(line.thick,line.alpha,line.color);
			
		o.graphics.beginFill(color,alpha);
		o.graphics.drawCircle(cx,cy,radius );
		o.graphics.endFill();
	}
	*/
}